apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: synology-homeassistant
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: synology
  resources:
    requests:
      storage: .5Gi
